-- v2 of mason , mason-lspconfig has a breaking change, so pinning the version to ^1.0.0 for now
return {
  { "mason-org/mason.nvim", version = "^1.0.0" },
  { "mason-org/mason-lspconfig.nvim", version = "^1.0.0" },
}
